name: my first workflow
on: push 
jobs:
    First_job: 
        runs-on: ubuntu-latest
        steps: 
        - name: welocme mesage
          run: echo "My first github actions"

        - name: list files
          run: ls -altr

        - name: read files
          run: cat README.md
        
          